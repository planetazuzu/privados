import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'emergency.form.clean',
  appName: 'emergency-form-clean',
  webDir: 'build'
};

export default config;
